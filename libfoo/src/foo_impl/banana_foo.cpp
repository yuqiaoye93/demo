#include "src/foo_impl/banana_foo.h"

namespace foo {

// bool exec(std::unordered_map<std::string id, T_ptr> input) {
//   prepare(input);
//   return true;
// };

// bool prepare(std::unordered_map<std::string id, T_ptr> input) { return true;
// };

}  // namespace foo